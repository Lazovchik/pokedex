const defaultState = {
    name: "",
    id: "",
    url: "",
    imgUrl: "",
    abilities: []
}

export const detailReducer = (state = defaultState, action) => {
    switch (action.type) {
        case (''):
    }
    return state;
}