const defaultState = {
    name: "",
    id: "",
    url: "",
    imgUrl: "",
    abilities: []
}

const detailReducer = (state = defaultState, action) => {
    switch (action.type) {
        case (''):
    }
    return state;
}